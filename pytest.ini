[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests
markers =
    live: тесты против живого окружения (нужен RUN_LIVE=1)
    live_http: локальные интеграционные тесты с реальным HTTP-стеком
